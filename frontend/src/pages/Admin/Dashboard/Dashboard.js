function Dashboard() {
    return (
        <div className='p-16' style={{
            minHeight: `calc(100vh - var(--admin-layout-header-height))`,
        }}>
            <div>
                Welcome back!
            </div>
        </div>
    );
}

export default Dashboard;